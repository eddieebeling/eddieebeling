{
  "name": "eddie-ebeling-portfolio",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate && node scripts/copy-static-to-dist.mjs",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "nuxt": "^3.19.0",
    "vue": "^3.5.0"
  },
  "type": "module",
  "devDependencies": {
    "sass": "^1.102.0"
  }
}
